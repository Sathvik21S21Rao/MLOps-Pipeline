---
- name: Ensure output directory for rendered k8s exists
  file:
    path: ./generated/k8s
    state: directory

- name: Render dataloader deployment manifest
  template:
    src: dataloader-deployment.yaml.j2
    dest: ./generated/k8s/dataloader-{{ dataloader_name | default('dataloader') }}.yaml
    mode: '0644'
